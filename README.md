# Jyotiranjan Beuria website

Static personal academic website for GitHub Pages.

## Local preview

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## GitHub Pages

The site is designed to publish from the root of the `main` branch in a repository named `jb`.
